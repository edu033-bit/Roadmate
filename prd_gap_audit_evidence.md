# PRD 대조 점검 근거 메모

## 배포 화면 관찰 — 2026-08-20

- 배포 도메인 `https://jeonnam-frei-god5cj9x.manus.space`의 DOM 텍스트에는 로드메이트 대시보드, 권장 출발 범위, 시간/비용 대안, 근거 지도, 외부 내비게이션 진입 문구가 존재했다.
- 같은 시점의 브라우저 스크린샷은 전체가 흰 화면으로 캡처되어, 실제 시각적 렌더링·가시성에 배포 품질 문제가 있을 가능성을 남긴다.
- 브라우저 콘솔에는 오류가 기록되지 않았다. 이 결과만으로 원인을 단정하지 않으며, 반응형·스타일 자산·실제 조작 흐름을 추가 확인한다.

## 배포 API 실측 — 2026-08-20

- `GET /api/plan/summary?live=true`는 200 응답을 반환했고, 기상청(KMA) 실황과 VMS·YGPA 출처를 포함했다. 다만 한국도로공사 실시간 통행시간과 통행료의 신뢰도는 각각 `insufficient`였으며, 응답 상세에는 두 엔드포인트가 구성되지 않았다고 표시됐다.
- `GET /api/plan/options?live=true`는 HGV 후보 3개와 경로 지오메트리를 반환했다. 각 후보의 `directCost`는 모두 `유류비 추정치 · 통행료 확인 필요`였고, 비용은 고정 경유 단가에 근거한 유류비만 계산됐다.
- 따라서 외부 API가 일부 정상 응답하더라도, PRD가 요구한 고속도로 ETA·차종별 통행료를 결합한 총 직접비 제공은 현재 응답만으로 충족되었다고 볼 수 없다.

## 개발 미리보기 화면 관찰 — 2026-08-20

- 데스크톱·390px 모바일 전체 화면 캡처 모두에서 초기 로딩 스켈레톤이 지속적으로 노출됐다. 원인은 이번 점검에서 확정하지 않았으나, Vite 로그에는 `maplibre-gl-worker.mjs` 최적화 의존성 파일이 없다는 경고가 남았다.
- 반면 배포 API 단독 호출은 정상 응답했다. 따라서 대시보드의 결과 전환·지도 초기화·스타일 로딩 중 어느 단계에서 문제가 나는지 별도 원인 분석이 필요하다.

## 최신 UI 교정 재점검 — 2026-08-20

- 작업 중 최신 체크포인트 `6a87be52`가 동기화되었다. 새 구현은 로딩 중에도 대체 후보를 즉시 유지하도록 바뀌었고, 390px 모바일 및 1280px 데스크톱 미리보기에서 권장 출발 범위·추천 카드·고정 비교 액션이 정상적으로 보였다.
- 따라서 앞선 스켈레톤 지속 노출과 빈 화면 관찰은 최신 버전의 잔여 결함으로 보고하지 않는다. 다만 최신 화면도 대체 후보와 고정 버퍼를 기반으로 결과를 먼저 그리는 구조이므로, 데이터 품질·실시간 연동 관련 갭은 그대로 남는다.

## 최신 공개 배포본 상호작용 — 2026-08-20

- 공개 도메인에서도 최신 UI가 보였고, API 완료 뒤 후보의 출발 시각·거리·유류비 추정값이 실시간 HGV 경로 응답으로 갱신됐다.
- 운행 조건 수정 시트에는 현재 위치 사용, 자유 텍스트 출발지·도착지, 2·3축 및 특수 차량 프리셋, 도착 희망 시각, 업무량 토글, 차량 상세 제원 토글이 실제로 표시됐다.
- 목적지 입력은 좌표 자동 정규화·주소 검색 결과 목록이 아니라 자유 텍스트와 7개 고정 빠른 선택 및 최근 선택에 의존한다. 자유 텍스트만 입력한 경우 좌표가 비워지고 서버가 기본 좌표를 사용하므로, 전남 전역 주소 입력 요구사항은 부분 구현으로 평가한다.

## 최신 후보 비교 화면 — 2026-08-20

- 체크포인트 `a7d5a743`에서 후보 비교 화면은 기본 추천·시간 우선·비용 우선의 선택 상태, 출발 시각, 예상 소요 시간, 예상 비용, 선택 경로 외부 내비게이션 열기와 계획 복귀를 모바일·데스크톱 모두에서 제공했다.
- 이 교정은 PRD의 설명 가능한 후보 비교와 56px급 하단 행동 영역 요구에 부합하는 개선이다. 단, 화면에 표시되는 예상 비용은 여전히 통행료가 제외된 유류비 추정치이고, 지도·위험 근거의 실시간 공간 결합은 구현되지 않았다.
