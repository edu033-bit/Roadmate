# 전남 화물 운행 플래너 — PRD 대비 배포본 구현 갭 감사

|항목|내용|
|---|---|
|점검 기준 PRD|`PRD_전남_화물_운행_플래너_MVP.md` v1.1|
|점검 대상|공개 배포본 및 최신 체크포인트 `a7d5a743`|
|점검 일시|2026-08-20 (KST)|
|판정 방법|활성 화면·REST API·외부 데이터 응답·서버 코드·자동 검증·모바일/데스크톱 화면을 대조|

## 결론

현재 배포본은 **‘출발 전 결정을 돕는 화면 골격’은 상당 부분 구현**되어 있다. 사용자는 현재 위치 사용, 차량 프리셋, 업무량 선택, 권장 출발 범위, 기본·시간·비용 후보 비교, 근거 지도, 외부 내비게이션 열기를 실제로 이용할 수 있다. 최신 비교 화면은 선택 경로와 복귀·내비 행동을 명확히 분리했고, 모바일과 데스크톱에서 큰 수치와 하단 행동 영역도 확인됐다.[1]

그러나 PRD가 약속한 **신뢰 가능한 사전 운행 의사결정 엔진**은 아직 완성되지 않았다. 특히 전남 전역 주소를 좌표로 정규화하는 입력 흐름, 한국도로공사 통행시간·통행료를 반영한 총 직접비, 기상특보·예보 기반의 고위험 판정, 실제 YGPA 시간대 패턴 적용, 데이터 장애 시의 정직한 상태 표시는 현재 배포본에서 미구현 또는 부분 구현이다. 따라서 이 버전은 화면 시연과 HGV 경로 초안에는 사용할 수 있으나, 비용·안전·출발시각 판단을 신뢰도 높게 제공하는 MVP 완료 상태로 보기는 어렵다.

> **우선순위 원칙:** 화면을 더 다듬기보다, 결과가 실제 입력·실시간 근거·정확한 비용에 기반한다는 것을 먼저 보장해야 한다. PRD v1.1에서 주유·캡처·카카오·SMS 공유는 MVP가 아닌 다음 스프린트 범위이므로, 현 시점의 P0 결함으로 집계하지 않았다.[2]

## 요구사항 판정 요약

|판정|개수|의미|
|---|---:|---|
|구현|5|PRD의 핵심 수용 기준을 현 배포 활성 경로에서 충족|
|부분 구현|21|UI 또는 연결은 있으나 데이터·오류 처리·세부 수용 기준이 부족|
|미구현|3|활성 경로에 필요한 기능이 없음|
|검증 불가|1|코드·테스트만으로 PRD의 운영 지표를 확인할 수 없음|

세부 항목별 판정은 별도 매트릭스에서 확인할 수 있다.[3]

## P0 — MVP 신뢰성을 위해 먼저 보완할 항목

|우선순위|구현 갭|현재 근거|필요한 완료 기준|
|---|---|---|---|
|P0-1|**주소 검색·좌표 정규화 부재**|출발지·목적지는 자유 텍스트와 7개 빠른 선택에 의존한다. 자유 텍스트를 바꾸면 좌표가 비워지고 서버는 기본 좌표를 사용한다.|주소 검색 결과를 선택해야만 계획 요청을 보내도록 하고, 위치 거부 시 최근 출발지·주소 검색만으로도 실제 선택 좌표가 경로 엔진에 전달되게 한다. 미정규화 입력은 명확히 오류 처리한다.|
|P0-2|**통행시간·통행료가 결과에 결합되지 않음**|실측 API에서 EX 통행시간·통행료는 `endpoint is not configured`로 반환됐다. 후보 비용은 고정 경유가 `1550원`을 쓴 유류비뿐이다.|한국도로공사 엔드포인트를 구성하고, 차종별 통행료와 구간 통행시간을 후보 계산에 반영한다. 불가 구간은 금액에 섞지 말고 `일부 구간 확인 필요`와 영향 범위를 표시한다.|
|P0-3|**기상 예보·특보 기반 안전 규칙 미구현**|현재는 KMA 초단기 실황의 풍속·강수만 임계치 판정한다. 단기예보, 기상특보, 적설은 판정에 쓰지 않는다.|기상특보와 운행 예정 시간대의 예보를 수집하고, 근거·기준시각·임계값을 함께 표시한다. 고위험이면 기본 추천을 숨기고 우회/계속 계획을 명시적으로 선택하도록 한다.|
|P0-4|**VMS·경로 위험의 공간 결합 부재**|VMS 호출 여부만으로 일반 안내 문구를 넣으며 메시지·위치·방향을 파싱하거나 후보 경로와 교차하지 않는다.|VMS 메시지를 정규화하고 후보 경로 버퍼와 교차해 해당 후보 카드와 지도에만 주의 근거를 표시한다. VMS는 법적 통제 판정이 아닌 안내라는 문구도 유지한다.|
|P0-5|**대체 수치가 최신 결과처럼 표시될 위험**|요청 실패 시 클라이언트가 오류를 숨기고 고정 후보를 유지한다. 근거 패널도 활성 API 출처가 아니라 고정 `최근` 목록을 사용한다.|대체 결과에는 `계산 예시/근거 부족` 상태와 생성 시각을 명확히 표시한다. 활성 응답의 출처·기준시각·신뢰도만 화면과 근거 패널에 전달하며, 실패 때는 경로 실패와 외부 내비 행동을 우선 제시한다.|
|P0-6|**총 직접비의 의미 불일치**|화면은 ‘예상 직접비’라고 표시하지만 통행료를 포함하지 않는다.|유류비와 확인 가능한 통행료를 합산한 경우에만 ‘총 직접비’로 표기한다. 통행료 미확인 상태에서는 유류비 추정과 통행료 확인 필요를 분리 표시한다.|

이 여섯 항목은 모두 PRD의 입력·HGV 후보·비용·안전·데이터 장애 수용 기준에 직접 연결된다.[2] 이 작업이 끝나기 전에는 후보 카드 디자인을 더 정교하게 만드는 것보다 **결과의 근거와 실패 상태를 정확히 전달하는 것**이 더 중요하다.

## P1 — P0 이후 품질을 높일 항목

|우선순위|구현 갭|권장 보완|
|---|---|---|
|P1-1|광양항 패턴이 실제 데이터와 무관하게 명칭 조건만으로 15분 가산|YGPA 응답을 파싱해 터미널·요일·시간대별 패턴으로 보정하고, 데이터 실패·부족 시에는 보정을 해제해 일반 전남권 계산으로 전환한다.|
|P1-2|차량 상세에서 길이·폭을 사용자가 수정할 수 없음|길이·폭을 상세 입력에 추가하고 유효 범위 검증을 거쳐 ORS HGV 요청과 비용 산식에 반영한다.|
|P1-3|운행 중 화면이 계획 당시 위험·ETA를 재사용|운행 중 재확인 진입 시 선택 경로 기준으로 위험·ETA를 다시 조회하고, 새로고침 기준시각과 다음 행동을 큰 글자로 표시한다.|
|P1-4|실제 위험 지도 오버레이 및 PRD 지도 아키텍처 미완성|MapLibre는 유지하되 PRD의 OpenFreeMap 전환 가능 설정을 만들고, 경로·VMS·기상 근거를 GeoJSON으로 지도에 렌더링한다.|
|P1-5|로컬 저장 정책의 사용자 고지 없음|설정 또는 입력 시트에 ‘이 기기 브라우저에만 저장되며 기기 변경·데이터 삭제 시 사라질 수 있음’을 명시한다.|
|P1-6|서버 관측성 미구현|API별 성공률, 응답시간, 마지막 갱신시각, 데이터 누락률을 개인정보·차량 상세 없이 기록한다.|
|P1-7|3초 첫 결과 SLA 검증 부재|실제 배포 환경에서 입력 완료부터 첫 후보 노출까지 측정하고, 느린 소스별 부분 갱신·타임아웃 기준을 테스트와 대시보드에 추가한다.|

## 현재 이미 갖춘 기반

|영역|현재 확인한 구현|
|---|---|
|차량·HGV|2·3축·특수 프리셋과 제원 전달, ORS `driving-hgv` 후보 3개 반환|
|결정 UI|권장 출발 범위, 기본·시간·비용 후보, 선택 상태, 큰 하단 행동|
|안전 흐름|고위험 Hero, 우회 경로 보기와 계속 계획의 명시 행동|
|로컬 경험|계획 입력·최근 목적지의 브라우저 로컬 저장|
|핸드오프|카카오맵·TMAP·네이버 지도 외부 내비게이션 딥링크|
|접근성 기본|상태 텍스트 병기와 큰 CTA, 모바일 중심 레이아웃|

## 검증 및 릴리스 판단

기존 자동 검증은 5개 파일, 8개 테스트와 타입 검사, 프로덕션 빌드를 통과했다. 다만 해당 테스트는 이전 tRPC 기반 서비스 계약에 집중되어 있으며, 실제로 활성인 `original/PlannerArtApp`과 `/api/plan/summary`, `/api/plan/options` REST 브리지를 직접 검증하지 않는다. 따라서 현재의 녹색 테스트 결과를 P0 데이터 연동·오류 처리 수용 기준의 통과 증거로 해석하면 안 된다.[1]

다음 릴리스에서는 최소한 다음을 자동화하는 것이 바람직하다.

|검증 시나리오|반드시 확인할 결과|
|---|---|
|정규화된 전남 주소 입력|선택 좌표가 ORS 요청에 그대로 전달되고 기본 좌표로 대체되지 않음|
|EX 통행시간·통행료 정상/실패|총 직접비 또는 명확한 확인 필요 상태가 카드·근거 패널에 동일하게 표시됨|
|기상특보·예보 고위험|기본 추천 숨김, 우회/계속 계획 선택, 출처·기준시각·면책 문구 표시|
|ORS/VMS/YGPA 개별 실패|고정 수치를 실시간으로 오인시키지 않고, 해당 데이터만 낮은 신뢰도로 전환|
|광양항/비광양항 비교|실제 항만 패턴 보정의 적용 여부·근거·오류 시 해제를 정확히 표시|
|운행 중 재확인|선택 경로의 최신 ETA·위험 정보를 다시 조회하고 기준시각을 표시|

## 범위 충돌 처리

초기 `PRD.md`에는 화면 캡처, 카카오 Link, SMS, 최저가 주유소 추천이 기능 요구사항으로 적혀 있다.[4] 반면 최신 상세 PRD v1.1은 이 기능들을 다음 스프린트(P2)로 이연하고 카카오·문자 공유를 MVP 비목표로 명시한다.[2] 본 감사는 버전 정보와 변경 이력이 있는 **v1.1을 우선 기준**으로 채택했다. 만약 초기 PRD를 별도 납품 계약 기준으로 사용한다면, 위 네 기능은 모두 미구현으로 추가해야 한다.

## 참고 자료

|번호|자료|위치|
|---:|---|---|
|[1]|배포 화면·API·자동 검증·상호작용 점검 근거|`prd_gap_audit_evidence.md`|
|[2]|전남 화물 운행 플래너 MVP PRD v1.1|사용자 작업 폴더 `PRD_전남_화물_운행_플래너_MVP.md`|
|[3]|PRD 요구사항별 구현 상태 매트릭스|`prd_requirements_matrix.md`|
|[4]|초기 광양항 스마트 배차 플래너 PRD|사용자 작업 폴더 `PRD.md`|
