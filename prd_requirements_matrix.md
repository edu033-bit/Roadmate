# PRD 요구사항 대조 매트릭스

> **판정 기준:** `PRD_전남_화물_운행_플래너_MVP.md` v1.1의 MVP/P0·P1 요구사항과 공개 배포본 `a7d5a743`을 대조했다. **구현**은 수용 기준을 현 배포본에서 충족한 경우, **부분 구현**은 UI·코드가 있으나 핵심 데이터·오류 처리·수용 기준이 빠진 경우, **미구현**은 활성 경로에 해당 기능이 없는 경우다.

|영역|PRD 요구사항|상태|배포본 근거 및 잔여 갭|
|---|---|---|---|
|출발지|선택형 위치 권한, 거부 시 주소 검색 또는 최근 출발지로 계획 완료|부분 구현|현재 위치 권한 거부 안내와 자유 입력은 있다. 그러나 주소를 좌표로 정규화하는 검색이 없고, 최근 **출발지**도 없다. 자유 텍스트 입력 시 좌표가 비워진다.|
|목적지|주소 검색·최근 목적지·광양항 빠른 선택, 좌표 정규화|부분 구현|최근 목적지와 GWCT·KIT 빠른 선택은 있다. 하지만 7개 고정 후보와 자유 텍스트가 전부이며, 자유 텍스트 주소의 지오코딩·좌표 정규화가 없다.|
|납기|큰 시간 UI, 누락·과거 시각 오류 설명|부분 구현|`type=time`과 과거 시각 오류는 있다. 날짜/운행일 개념과 서버 입력 검증은 없고, 시간만으로 당일·익일을 구분하지 못한다.|
|차량 프리셋|2축·3축·4축 이상/특수 및 최초 1회 상세 확인|구현|세 프리셋과 기본 제원·브라우저 저장을 제공한다.|
|차량 상세|길이·폭·높이·축중·총중량·위험물·평균 연비 수정 및 HGV/비용 반영|부분 구현|높이·총중량·축중·연비·위험물만 화면에서 수정한다. 길이와 폭은 요청에는 포함되지만 사용자가 수정할 수 없다.|
|로컬 저장·고지|프로필·최근 목적지를 로컬 저장하고 기기 변경/삭제 시 소실 고지|부분 구현|계획 입력과 최근 목적지는 `localStorage`에 저장한다. 설정 화면 또는 소실 고지는 없다.|
|HGV 후보 경로|차량 제약을 반영한 최소 2개 HGV 후보|구현|서버가 ORS `driving-hgv` 요청에 차량 제원을 전달하고, 공개 API는 3개 후보를 반환했다.|
|후보의 의미|기본·시간 우선·비용 우선의 실제 비교|부분 구현|세 카드와 선택 UX는 구현됐다. 다만 ORS 대안을 순서대로 레이블링하며, 통행료가 없는 유류비만으로 비용 우선을 판정한다. 업무량도 후보 생성 조건이 아니라 기본 선택에만 주로 쓰인다.|
|권장 출발 범위|이동시간·안전 버퍼·광양항 보정으로 산출된 범위|부분 구현|범위 UI와 ORS 이동시간 역산은 있다. 안전 버퍼는 업무량별 고정값이고, 기상·VMS·YGPA 응답 내용에 따라 가산되지 않는다.|
|신뢰도|최신성·출처·고속도로 반영·항만 보정을 표시|부분 구현|근거 패널은 존재하지만 활성 화면이 API별 `sources`를 전달하지 않아 고정 `최근` 출처 목록을 보여 준다.|
|유류비|거리 ÷ 개인 연비 × 오피넷 경유가|부분 구현|개인 연비와 경로 거리를 쓴다. 경유 단가는 코드에 `1550`으로 고정되어 오피넷 연동이 아니다.|
|통행료|차종별 한국도로공사 요금 적용|미구현|공개 API 응답에서 EX 통행료 엔드포인트가 미구성 상태였다. 화면 비용은 통행료를 포함하지 않는다.|
|총 직접비|유류비 + 확인 가능한 통행료, 불가 구간은 확인 필요|부분 구현|`확인 필요` 문구는 있으나 표시 금액은 유류비 추정치뿐이다. 총 직접비라는 수용 기준을 충족하지 못한다.|
|기상 위험|단기예보·특보·강수·풍속·적설을 근거로 안전 버퍼/고위험 판단|부분 구현|KMA 초단기 **실황**의 풍속·강수만 임계치 판정에 쓴다. 단기예보·기상특보·적설은 사용하지 않는다.|
|VMS 위험|경로 인근 VMS를 주의 근거로 표시|부분 구현|VMS 호출은 있으나 메시지·위치·방향을 파싱하거나 경로와 공간 교차하지 않는다. 응답 가능 여부만으로 일반 안내 문구를 넣는다.|
|고위험 UX|기상 고위험이면 기본 추천을 숨기고 우회/계속 계획을 명시 선택|부분 구현|고위험 Hero와 두 선택지는 구현됐다. 다만 고위험 입력 근거가 실황 임계치에 한정돼 PRD의 특보·예보 기반 수용 기준을 충족하지 못한다.|
|광양항 보정|과거 패턴을 읽어 버퍼 반영, 오류 시 일반 경로로 전환|부분 구현|광양항 명칭이면 무조건 15분을 더한다. YGPA 응답을 해석·시간대별 반영하지 않으며, 오류·자료 부족 때도 보정을 해제하지 않는다.|
|결정 우선 UI|Hero, 후보 비교, 지도, 큰 하단 행동|구현|모바일·데스크톱에서 권장 출발 범위, 3개 후보 비교, 큰 내비/복귀 행동을 확인했다.|
|시각·접근성|상태를 색상 외 텍스트로 병기하고 56px 이상 CTA|구현|정상·주의·근거 부족 텍스트와 큰 CTA가 있다. 다만 기본 테마는 PRD의 다크 네이비 정책과 다르다.|
|근거 지도|MapLibre·OpenFreeMap·GeoJSON 위험 오버레이|부분 구현|MapLibre 경로 지도는 있다. 베이스맵은 CARTO이며 OpenFreeMap이 아니고, 실제 VMS·기상 위험 좌표 오버레이는 없다.|
|외부 내비|선택 경로를 외부 내비게이션으로 열기|구현|카카오맵·TMAP·네이버 지도 딥링크를 제공한다.|
|운행 중 재확인|저장한 선택 경로의 ETA·현재 고위험·다음 조치를 큰 글자로 재확인|부분 구현|운행 중 화면은 있으나 계획 시점의 상태를 재사용하고, 진입 시 API 재조회·최신 ETA 갱신을 하지 않는다.|
|ORS 장애|실패 시 실패를 명시하고 이전/모의 결과를 최신처럼 표시하지 않음|미구현|서버는 503을 반환하지만 클라이언트는 오류를 삼키고 고정 수치 대체 후보를 계속 표시한다.|
|기상·VMS 지연|기준시각·지연 상태를 표시하고 결측 시 안전 판정을 낮춤|부분 구현|서버 응답에는 출처 기준시각이 있으나 활성 화면은 이를 사용하지 않는다. 고정 `오늘 13:02`·`최근` 표기가 남는다.|
|항만·통행료 장애|항만 보정 해제, 통행료는 0원 대신 확인 필요|부분 구현|통행료를 0원으로 보이지는 않지만, 항만 보정은 오류와 무관하게 적용된다.|
|저속망|기본 UI 우선, 단계별 갱신·스켈레톤·데이터 상태 표시|부분 구현|대체 후보를 즉시 표시하도록 개선됐다. 그러나 대체 수치와 실시간 결과의 구분·단계별 데이터 상태 표시는 없다.|
|보안·개인정보|키 비노출, 서버 이력 미저장, 위치 선택|부분 구현|활성 요청은 서버 프록시를 사용하고 계획·차량 저장 테이블도 없다. 다만 로컬 저장의 소실 고지와 요청 데이터의 관측성 마스킹 정책은 구현 증거가 없다.|
|관측성|API별 성공률·응답시간·갱신시각·누락률 서버 기록|미구현|활성 서버 코드에 메트릭·성공률·지연시간·누락률 기록이 없다.|
|성능|즉시 로딩 후 가능 시 3초 내 첫 후보, 부분 결과 갱신|검증 불가|빌드·타입·기존 테스트는 통과했지만 최신 배포본의 3초 SLA를 측정하거나 지속 관측하는 테스트는 없다.|
|라이선스|ORS·MapLibre·Turf·H3·OSM/OpenFreeMap 출처·체크리스트|부분 구현|MapLibre/OSM/CARTO 지도 출처는 화면에 보인다. PRD의 OpenFreeMap·Turf·H3 구성과 릴리스 체크리스트는 확인되지 않는다.|

## 범위 해석 메모

초기 `PRD.md`에는 캡처·카카오톡·SMS 공유와 주유소 추천이 들어 있다. 하지만 최신 v1.1 PRD는 주유·휴식·공유를 **다음 스프린트(P2)** 로 옮기고, 카카오·문자 공유는 MVP 비목표로 명시했다. 따라서 이 항목들은 현재 MVP의 미구현 결함으로 집계하지 않았다. 다만 초기 PRD를 별도 계약 기준으로 삼는다면 캡처, 카카오 Link, SMS URI, 오피넷 최저가 주유소 노출은 모두 활성 배포 경로에 없다.
